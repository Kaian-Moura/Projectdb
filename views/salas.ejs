<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Salas Disponíveis - Sistema de Reservas</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/salas.css">
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Salas Disponíveis</h1>
      </header>

      <!-- Adicionando seção de filtro -->
      <section class="filter-section">
        <h2 class="filter-title">Filtrar Salas</h2>
        <div class="filter-form">
          <div class="filter-group">
            <label for="capacidade">Capacidade Mínima:</label>
            <select id="capacidade">
              <option value="1">1 pessoa</option>
              <option value="2">2 pessoas</option>
              <option value="5">5 pessoas</option>
              <option value="10">10 pessoas</option>
              <option value="20">20 pessoas</option>
            </select>
          </div>
          <div class="filter-group">
            <label for="dataReserva">Data Desejada:</label>
            <input type="date" id="dataReserva">
          </div>
          <div class="filter-buttons">
            <button id="aplicarFiltro" class="filter-btn">Aplicar Filtros</button>
            <button id="limparFiltro" class="filter-btn clear-btn">Limpar</button>
          </div>
        </div>
      </section>

      <div class="rooms-container" id="roomList">
        <!-- Salas serão carregadas dinamicamente via JavaScript -->
        <% if (salas && salas.length > 0) { %> <% salas.forEach(function(sala) {
        %>
        <div class="room-card">
          <div class="room-image"><%= sala.nome %></div>
          <div class="room-info">
            <h3><%= sala.nome %></h3>
            <p><strong>Capacidade:</strong> <%= sala.capacidade %> pessoas</p>
            <p><strong>Localização:</strong> <%= sala.localizacao %></p>
            <div class="room-features">
              <% if (sala.recursos) { const recursos = sala.recursos.split(',');
              recursos.forEach(function(recurso) { %>
              <span class="feature"><%= recurso.trim() %></span>
              <% }); } %>
            </div>
          </div>
          <button class="reserve-btn" data-id="<%= sala.id %>">Disponível</button>
        </div>
        <% }); %> <% } else { %>
        <p>Nenhuma sala disponível no momento.</p>
        <% } %>
      </div>

      <div class="navigation">
        <a href="/" class="home-link">Voltar para página inicial</a>
      </div>
    </div>

    <script src="/js/salas.js"></script>
  </body>
</html>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Salas Disponíveis</h1>
      </header>

      <div class="rooms-container" id="roomList">
        <!-- Salas serão carregadas dinamicamente via JavaScript -->
        <% if (salas && salas.length > 0) { %> <% salas.forEach(function(sala) {
        %>
        <div class="room-card">
          <div class="room-image"><%= sala.nome %></div>
          <div class="room-info">
            <h3><%= sala.nome %></h3>
            <p><strong>Capacidade:</strong> <%= sala.capacidade %> pessoas</p>
            <p><strong>Localização:</strong> <%= sala.localizacao %></p>
            <div class="room-features">
              <% if (sala.recursos) { const recursos = sala.recursos.split(',');
              recursos.forEach(function(recurso) { %>
              <span class="feature"><%= recurso.trim() %></span>
              <% }); } %>
            </div>
          </div>
          <button class="reserve-btn" data-id="<%= sala.id %>">Disponível</button>
        </div>
        <% }); %> <% } else { %>
        <p>Nenhuma sala disponível no momento.</p>
        <% } %>
      </div>

      <div class="navigation">
        <a href="/" class="home-link">Voltar para página inicial</a>
      </div>
    </div>

    <script src="/js/salas.js"></script>
  </body>
</html>
